import "./Intro.css";

const Intro = () => {
    return (
        <div className="intro">
            <h1>Welcome to Your To-Do List</h1>
            <p>Add tasks to your list, check them off when done, or delete them as needed!</p>
        </div>
    );
};

export default Intro;
